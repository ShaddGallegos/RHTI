servers:
- name: "{{ newnodez }}"
  location: DENVER
  organization: REDHAT
  hostgroup: node
  interfaces_attributes:
  - identifier: eth0
    type: interface
    managed: true
    primary: true
    provision: true
    subnet: 10.168.0.0
    name: "{{ newnodez }}"
    domain: test.spg
    autostart: enable
  build: true
  enabled: true
  provision_method: build 
  managed: true
