# approval-gate.yml
#
# This playbook automates steps to be taken at a workflow approval gate.
#
# See documentation for the `approval-gate` role for more information on
# requirements and parameters.
---
- hosts: localhost
  connection: local
  gather_facts: no
  become: no
  roles:
    - approval-gate
