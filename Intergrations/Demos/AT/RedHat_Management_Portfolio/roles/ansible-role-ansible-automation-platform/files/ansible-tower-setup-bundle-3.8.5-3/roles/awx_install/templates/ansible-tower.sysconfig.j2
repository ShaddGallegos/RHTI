# The settings in this file are used by the ansible-tower-service script.

# List of services managed by Ansible Tower (space-delimited).
{% if awx_install_database == 'external' %}
TOWER_SERVICES="{{ awx_install_redis_init_name }} nginx supervisord"
{% else %}
TOWER_SERVICES="{{ awx_install_pg_init_name }} {{ awx_install_redis_init_name }} nginx supervisord"
{% endif %}
