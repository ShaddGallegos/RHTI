---
# virtual environments and compute resources 

    - name: Install packages for general via pip 
      pip:
        name:
          - bottle
          - requests
          - requests-credssp
          - pywinrm
          - ansible-tower-cli
          - apypie
          - PyYAML
          - packaging
          - xmltodict
          - msrest
        umask: "0022"
        state: forcereinstall
        extra_args: --user

    - name: Install packages for satellite via pip 
      pip:
        name:

    - name: Install packages for vmware via pip 
      pip:
        name:
         - pywinrm
         - azure
         - azure-cli-core
         - azure-cli-nspkg
         - azure-common
         - azure-mgmt-apimanagement
         - azure-mgmt-authorization
         - azure-mgmt-batch
         - azure-mgmt-cdn
         - azure-mgmt-compute
         - azure-mgmt-containerinstance
         - azure-mgmt-containerregistry
         - azure-mgmt-containerservice
         - azure-mgmt-datalake-store
         - azure-mgmt-dns
         - azure-mgmt-keyvault
         - azure-mgmt-marketplaceordering
         - azure-mgmt-monitor
         - azure-mgmt-managedservices
         - azure-mgmt-managementgroups
         - azure-mgmt-network
         - azure-mgmt-nspkg
         - azure-mgmt-privatedns
         - azure-mgmt-redis
         - azure-mgmt-resource
         - azure-mgmt-rdbms
         - azure-mgmt-search
         - azure-mgmt-servicebus
         - azure-mgmt-sql
         - azure-mgmt-storage
         - azure-mgmt-trafficmanager
         - azure-mgmt-web
         - azure-nspkg
         - azure-keyvault
         - azure-graphrbac
         - azure-mgmt-cosmosdb
         - azure-mgmt-hdinsight
         - azure-mgmt-devtestlabs
         - azure-mgmt-loganalytics
         - azure-mgmt-automation
         - azure-mgmt-iothub
         - azure-mgmt-recoveryservices
         - azure-mgmt-recoveryservicesbackup
         - azure-mgmt-notificationhubs
         - azure-mgmt-eventhub
        umask: "0022"
        state: forcereinstall
        extra_args: --user

    - name: Install packages for vmware via pip 
      pip:
        name:
         - aws
         - boto3
         - boto
         - botocore
         - awscl
        umask: "0022"
        state: forcereinstall
        extra_args: --user

    - name: Install packages for Google Compute Engine via pip 
      pip:
        name:
         - gce
         - gce-api
        umask: "0022"
        state: forcereinstall
        extra_args: --user

    - name: Install packages for vmware via pip 
      pip:
        name:
         - PyVmomi
        umask: "0022"
        state: forcereinstall
        extra_args: --user